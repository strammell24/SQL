
#1 Place the items that are the most liked to least liked 

SELECT item_name, average_rating
FROM superstore
ORDER BY average_rating desc;

#2 Place the store items from the least expensive to the most expensive. 

SELECT item_name, price
FROM superstore
ORDER BY price asc

#3 What is the most expensive item (and its price) in the Kitchen Supplies category? 

SELECT MAX(item_name), price
FROM superstore 
WHERE category = "Kitchen Supplies";

#4 How many store items are in each category?

SELECT COUNT(item_id), category
FROM superstore
GROUP BY category;


